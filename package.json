{
	"name": "yot",
	"type": "module",
	"scripts": {
		"dev": "cargo run --bin yot-server",
		"build": "cargo build --release && npm --prefix web run build",
		"web:dev": "npm --prefix web run dev",
		"web:build": "npm --prefix web run build",
		"start": "target/release/yot-server",
		"mcp": "target/release/yot-mcp",
		"test": "cargo test"
	}
}
